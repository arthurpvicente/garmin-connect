# requirements.txt
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
garminconnect==0.2.38
python-jose[cryptography]==3.3.0
cryptography==43.0.3
apscheduler==3.10.4
redis[hiredis]==5.1.1
pytest==8.3.3
pytest-asyncio==0.24.0
fpdf2==2.7.9
# RAG assistant (httpx is already in requirements above; pgvector adds the Vector column type)
pgvector==0.4.2
# LangGraph agent (Phase 2) — routes questions between SQL stats and semantic search
langgraph>=0.2.0
langchain-google-genai>=2.0.0
# MCP server (Phase 3) — exposes tools to Claude Desktop and other MCP clients
mcp>=1.0.0
# Pin Starlette — mcp pulls sse-starlette which upgrades Starlette to 1.x, breaking FastAPI 0.115
starlette>=0.37.2,<0.42.0
